<li id="post-<%= post.id %>" class="wow slideInLeft">
	<div>
<div id="gg">
		<p><%= post.content %></p>
		<br>


		</div>
		&nbsp
		<% if user_signed_in? && post.user_id ==  current_user.id %>
		<br>
		<%= link_to "Delete", destroy_post_path(post.id), method: :delete, remote: true %>
		&nbsp&nbsp&nbsp
		<%= link_to "Edit", edit_post_path(post.id),method: :get%>


		<% end %>

		<% if user_signed_in? %>
		<p>
			<sub id="post-<%= post.id %>-likes" class="post-like-status">
				<%= render 'post_like_link', post: post %>
			</sub>
		</p>
		<% else %>
		<sub>
				<%= post.likes.length %> Likes
		
			</sub>

		<% end %>

		<div>
			<%= render 'comments/post_comment', post: post %>
		</div>
		
	</div>
</li>


